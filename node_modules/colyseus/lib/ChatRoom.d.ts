import { Room } from "./index";
export declare class ChatRoom extends Room<any> {
    constructor(options: any);
    onJoin(client: any): void;
    onLeave(client: any): void;
    onMessage(client: any, data: any): void;
    onDispose(): void;
}
