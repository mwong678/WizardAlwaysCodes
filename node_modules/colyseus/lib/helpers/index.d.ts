/**
 * EntityMap type
 * A map of string id to type.
 */
export declare type EntityMap<T> = {
    [entityId: string]: T;
};
/**
 * Generate a random string id (length=7)
 * @return {string}
 */
export declare function generateId(): string;
export { nonenumerable } from "nonenumerable";
