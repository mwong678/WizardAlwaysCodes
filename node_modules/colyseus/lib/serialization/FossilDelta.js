"use strict";
var msgpack = require("msgpack-lite");
var FossilDelta = (function () {
    function FossilDelta() {
    }
    FossilDelta.prototype.setData = function (data) {
        // ensure state is populated for `sendState()` method.
        this._previousState = data;
        this._previousStateEncoded = msgpack.encode(this._previousState);
    };
    FossilDelta.prototype.getPatches = function () {
    };
    return FossilDelta;
}());
exports.FossilDelta = FossilDelta;
