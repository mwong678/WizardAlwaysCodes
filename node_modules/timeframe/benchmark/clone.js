"use strict";

console.log('Please wait...')

var Benchmark = require('benchmark')
  , suite = new Benchmark.Suite()
  , obj = {
    entities: {
      one: { id: "1279819x918", x: 100, y: 400.50 },
      two: { id: "1279x918", x: 1250.3, y: 390.50 },
      three: { id: "1218", x: 80, y: 414.1 },
      four: { id: "1278x8", x: 96, y: 404.8 },
    }
  }

suite.add('JSON.parse( JSON.stringify )', function() {
  var cloned = JSON.parse( JSON.stringify( obj ) )
})

suite.add('Object.assign', function() {
  var cloned = Object.assign({}, obj)
})

suite.on('cycle', function(event) {
  console.log(String(event.target));
})

suite.run()
